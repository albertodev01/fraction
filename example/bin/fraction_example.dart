import 'package:fraction_example/fraction_example.dart' as fraction_example;

/// The app's main entrypoint.
void main(List<String> arguments) {
  // Creates a new console for standard I/O processing.
  final console = fraction_example.Console();

  // Runs the console.
  console.run();
}
